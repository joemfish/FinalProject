spring:
  datasource:
    username: bookshelf
    password: bookshelf
    url: jdbc:mysql://localhost:3306/bookshelf
    
  jpa:
    hibernate:
       ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    
  sql:
    init:
       mode: never